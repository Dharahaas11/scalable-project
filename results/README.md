# Generated results

Running the local batch, speed, serving, and graph scripts creates CSV files in
this folder. These generated files are intentionally ignored by Git by default;
keep selected small outputs only if they are needed as submission evidence.
